n1: float = float(input('Digite a primeira nota: '))
n2: float = float(input('Digite a segunda nota: '))
n3: float = float(input('Digite a terceira nota: '))
n4: float = float(input('Digite a quarta nota: '))

media = (n1 + n2 + n3 + n4) / 4

if media >= 0:
print(f'Sua média é {media:.2f} ')
